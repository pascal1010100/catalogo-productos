generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("mongodb+srv://jose:<Allthewitches1>@cluster0.vozzr.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0")
}

model Product {
  id             String  @id @map("_id")
  name           String
  model         String
  priceMayorista Float
  pricePublico   Float
  minCompra      String
  image          String
  createdAt      DateTime @default(now())
}

